steps:
  - name: build
    image: debian
    commands:
      - echo "This is the build step"
      - echo ${TEST}
    secrets: [ test ]
  - name: a-test-step
    image: debian
    commands:
      - echo "Testing.."